# vercel upload ignore (takes precedence over .gitignore, so essentials are mirrored here)
node_modules/
.next/
out/
.env*.local
.vercel
*.log
.DS_Store
# local tooling: graphify junction/symlink + harness logs must never deploy
# (bare name catches the link itself; slashed form catches dir contents)
.codegraph
.codegraph/
harness*.txt
test/screenshots/
